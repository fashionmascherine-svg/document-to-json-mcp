{
  "title": "Document-to-JSON Converter",
  "description": "Enter a public PDF URL to extract structured data. Supports invoices, bank statements, contracts, and generic documents. Powered by DeepSeek-v4-flash AI.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "file_url": {
      "title": "PDF URL",
      "type": "string",
      "description": "Public URL of the PDF file to process (max 20MB). The PDF must be publicly accessible."
    },
    "document_type": {
      "title": "Document Type",
      "type": "string",
      "description": "Type of document to extract. Each type has a different price and specific output format.",
      "editor": "select",
      "default": "invoice",
      "enum": [
        { "value": "invoice", "label": "Invoice ($0.02)" },
        { "value": "bank_statement", "label": "Bank Statement ($0.03)" },
        { "value": "contract", "label": "Contract ($0.05)" },
        { "value": "generic", "label": "Generic Document ($0.01)" }
      ]
    },
    "language": {
      "title": "Document Language(s)",
      "type": "string",
      "description": "Languages for OCR, separate with +. ISO codes: ita, eng, spa, fra, deu, por.",
      "default": "ita+eng"
    },
    "validate_totals": {
      "title": "Validate Totals (invoices only)",
      "type": "boolean",
      "description": "Checks that line item sums match declared totals.",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": ["file_url"]
}
