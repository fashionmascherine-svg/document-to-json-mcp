{
  "title": "Document-to-JSON Converter",
  "description": "Turn any PDF into structured JSON in seconds. No coding needed. Just paste a URL and get clean, organized data.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "file_url": {
      "title": "PDF URL",
      "type": "string",
      "description": "Paste the public link to your PDF file. Supports Dropbox, Google Drive, any web server. Max 20MB.",
      "editor": "textfield"
    },
    "document_type": {
      "title": "Document type",
      "type": "string",
      "description": "What type of document are you processing?",
      "editor": "select",
      "default": "invoice",
      "enum": ["invoice", "bank_statement", "contract", "generic"]
    },
    "language": {
      "title": "Language",
      "type": "string",
      "description": "Document language for better accuracy. Use ISO codes separated by +. Default: ita+eng",
      "editor": "textfield",
      "default": "ita+eng"
    },
    "validate_totals": {
      "title": "Validate totals",
      "type": "boolean",
      "description": "Check if line item sums match declared totals. Recommended for invoices.",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": ["file_url"]
}
